Configure foundational persistence infrastructure using EF Core and SQL Server.

Requirements:

EF Core integration
DbContext setup
local SQL Server configuration
migration support
connection string configuration
Initial entities:

EngineeringArtifact
ADR
Repository
Acceptance Criteria:

database migrations run successfully
database can be created locally
entities persist correctly
configuration supports Azure SQL migration later